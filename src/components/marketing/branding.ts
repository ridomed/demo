/** Public product branding, independent of the operational company settings. */
export const marketingBrand = {
  name: "sytemano",
  shortName: "STM",
  /** Set a public image path here when the real system screenshot is ready. */
  systemPreviewImage: "/marketing/system.png" as string | null,
  icon: "/marketing/logoSTM.png",
} as const;
